<?php
/**
 * 沙盒环境配置页面（内容模板）
 *
 * @var \Weline\Framework\View\Template $this
 */
?>

<div class="row">
    <div class="col-12">
        <div class="page-title-box d-sm-flex align-items-center justify-content-between">
            <h4 class="mb-sm-0"><lang>沙盒环境配置</lang></h4>
            <div class="page-title-right">
                <breadcrumb model='Weline\Backend\Model\Menu' order_field='order'
                            name_field='title'></breadcrumb>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-12 col-sm-12">
        <div class="card">
            <div class="card-body">
                <lang>沙盒环境是一套数据隔离的可提供给测试人员对代码进行测试的环境，测试时数据不会写入正式数据库，而是写入沙盒数据库。</lang>
            </div>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-12 col-sm-12">
        <div class="card">
            <div class="card-title">
                <div class='row'>
                    <template>Weline_Component::message.phtml</template>
                </div>
            </div>
            <div class="card-body">
                <h4 class='card-title'><lang>可配置参数：</lang></h4>
                <p class='card-title-desc'><lang>控制沙盒环境参数来达到测试系统环境。</lang></p>
                <div class="row">
                    <h3><lang>沙盒启动Key</lang></h3>
                    <div class="mt-2">
                        <lang>当前沙盒状态：</lang>
                        <span class="badge bg-@if{enabled=>'success'|'info'}"><lang>@if{enabled=>'开启'|'关闭'}</lang></span>
                    </div>
                    <div class="mt-2">
                        <lang>当前Key：</lang>
                        <span class="badge bg-info">{{env.sandbox_key}}</span>
                    </div>
                    <div class="mt-3">
                        <form class="row g-2 align-items-end" method="post"
                              action="@url{'*/admin/sandbox/setSandboxKey'}" novalidate>
                            <div class="col-md-6 col-lg-5">
                                <label class="form-label mb-1" for="sandbox_key_input"><lang>沙盒Key</lang></label>
                                <input type="text" class="form-control" name="key" id="sandbox_key_input"
                                       placeholder="@lang{请输入沙盒Key}" value="{{env.sandbox_key}}" required>
                            </div>
                            <div class="col-auto pb-1">
                                <button type="submit" class="btn btn-primary"><lang>保存</lang></button>
                            </div>
                        </form>
                    </div>

                    <div class="mt-2">
                        <lang>沙盒启动Key配置。使用时在请求后面加上sandbox参数。示例：</lang>
                        @frontend-url{'*/sandbox/enable'}?enable=on&key=<span class="text-info">{{ env.sandbox_key }}</span></span>
                        <a href="@backend-url{'*/admin/sandbox/enable'}?enable=on&key={{env.sandbox_key}}">
                            <lang>开启沙盒</lang>
                        </a>
                    </div>
                    <div class="mt-2">
                        <lang>关闭沙盒请在Cookie中删除或者设置：w_sandbox值。示例：</lang>
                        @frontend-url{'*/sandbox/close'}?close=on&key=<span class="text-info">{{ env.sandbox_key }}</span></span>
                        <a href="@backend-url{'*/admin/sandbox/close'}?close=on&key={{env.sandbox_key}}">
                            <lang>关闭沙盒</lang>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
