<?php
/**
 * 开发文档列表（内容模板）
 * 
 * 由布局系统自动包裹 HTML 框架
 *
 * @var \Weline\Framework\View\Template $this
 */
?>
<style>
.document-card {
    transition: background-color 0.35s ease, color 0.35s ease, box-shadow 0.35s ease, transform 0.35s ease, border-color 0.35s ease;
    border-radius: 8px;
    overflow: hidden;
    background: var(--backend-color-card-bg, #ffffff);
    border: 1px solid var(--backend-color-border-light, rgba(233, 236, 239, 0.6));
    color: var(--backend-color-text-primary, #2f3a52);
    box-shadow: var(--backend-shadow-sm, 0 1px 4px rgba(31, 45, 61, 0.02));
    position: relative;
}
.document-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, rgba(var(--backend-color-primary-rgb, 85, 110, 230), 0.3) 0%, rgba(249, 195, 233, 0.3) 100%);
    opacity: 0;
    transition: opacity 0.35s ease;
}
.document-card:hover {
    box-shadow: var(--backend-shadow-md, 0 4px 12px rgba(31, 45, 61, 0.08));
    transform: translateY(-2px);
    border-color: rgba(var(--backend-color-primary-rgb, 85, 110, 230), 0.2);
}
.document-card:hover::before {
    opacity: 1;
}
.document-header {
    background: linear-gradient(135deg, rgba(var(--backend-color-primary-rgb, 85, 110, 230), 0.06) 0%, rgba(249, 195, 233, 0.08) 100%);
    color: var(--backend-color-text-primary, #495057);
    padding: 0.75rem 1rem;
    border-bottom: 1px solid var(--backend-color-border-light, rgba(233, 236, 239, 0.5));
    transition: background-color 0.35s ease, color 0.35s ease, border-color 0.35s ease;
    position: relative;
}
.document-header h5,
.document-header h6,
.document-header .h5,
.document-header .h6 {
    color: var(--backend-color-text-primary, #495057);
    margin: 0;
    font-weight: 600;
    font-size: 0.95rem;
}
.document-body {
    padding: 0.875rem 1rem;
    background: var(--backend-color-card-bg, #ffffff);
    color: var(--backend-color-text-primary, #495057);
    transition: background-color 0.35s ease, color 0.35s ease;
}
.document-footer {
    padding: 0.5rem 1rem;
    background: var(--backend-color-bg-secondary, #f8f9fa);
    border-top: 1px solid var(--backend-color-border-light, rgba(233, 236, 239, 0.5));
    color: var(--backend-color-text-primary, #495057);
    transition: background-color 0.35s ease, border-color 0.35s ease, color 0.35s ease;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem;
}
.badge-module {
    background: linear-gradient(135deg, rgba(240, 147, 251, 0.15) 0%, rgba(245, 87, 108, 0.15) 100%);
    color: var(--backend-color-text-secondary, #6c757d);
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-size: 0.75rem;
    border: 1px solid rgba(240, 147, 251, 0.2);
}
.badge-auto {
    background: linear-gradient(135deg, rgba(79, 172, 254, 0.15) 0%, rgba(0, 242, 254, 0.15) 100%);
    color: var(--backend-color-text-secondary, #6c757d);
    padding: 0.25rem 0.5rem;
    border-radius: 4px;
    font-size: 0.75rem;
    border: 1px solid rgba(79, 172, 254, 0.2);
}
.summary-text {
    color: var(--backend-color-text-tertiary, rgba(108, 117, 125, 0.75));
    font-size: 0.8rem;
    line-height: 1.5;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
}
.action-buttons .btn {
    margin-right: 0.375rem;
    margin-bottom: 0;
    font-size: 0.8125rem;
    padding: 0.25rem 0.625rem;
    border-width: 1px;
    opacity: 0.85;
    transition: opacity 0.2s ease, transform 0.2s ease;
}
.action-buttons .btn:hover {
    opacity: 1;
    transform: translateY(-1px);
}
.action-buttons .btn-primary {
    background-color: transparent;
    border-color: var(--backend-color-text-secondary, #6c757d);
    color: var(--backend-color-text-secondary, #6c757d);
}
.action-buttons .btn-primary:hover {
    background-color: var(--backend-color-text-secondary, #6c757d);
    border-color: var(--backend-color-text-secondary, #6c757d);
    color: var(--backend-color-text-inverse, #fff);
}
.action-buttons .btn-info {
    background-color: transparent;
    border-color: var(--backend-color-text-secondary, #6c757d);
    color: var(--backend-color-text-secondary, #6c757d);
}
.action-buttons .btn-info:hover {
    background-color: var(--backend-color-text-secondary, #6c757d);
    border-color: var(--backend-color-text-secondary, #6c757d);
    color: var(--backend-color-text-inverse, #fff);
}
.action-buttons .btn-danger {
    background-color: transparent;
    border-color: var(--backend-color-danger, #dc3545);
    color: var(--backend-color-danger, #dc3545);
}
.action-buttons .btn-danger:hover {
    background-color: var(--backend-color-danger, #dc3545);
    border-color: var(--backend-color-danger, #dc3545);
    color: var(--backend-color-text-inverse, #fff);
}
.offcanvas-document {
    max-width: 1600px;
}
@media (min-width: 1920px) {
    .offcanvas-document {
        max-width: 1800px;
    }
}
@media (min-width: 1400px) and (max-width: 1919px) {
    .offcanvas-document {
        max-width: 1600px;
    }
}
@media (min-width: 1200px) and (max-width: 1399px) {
    .offcanvas-document {
        max-width: 1400px;
    }
}
@media (max-width: 991.98px) {
    .offcanvas-document {
        max-width: 90vw;
    }
}
@media (max-width: 575.98px) {
    .offcanvas-document {
        max-width: 100vw;
    }
}
.document-content {
    max-height: 60vh;
    overflow-y: auto;
    overflow-x: auto;
    padding: 1rem;
    background: var(--backend-color-bg-secondary, #f8f9fa);
    border-radius: 8px;
    white-space: pre-wrap;
    word-wrap: break-word;
    font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
    font-size: 14px;
    line-height: 1.6;
    margin: 0;
    border: 1px solid var(--backend-color-border-default, #e9ecef);
}
.document-content img {
    max-width: 100%;
    height: auto;
}
.document-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
    padding: 1rem;
    background: var(--backend-color-bg-secondary, #f8f9fa);
    border-radius: 8px;
    margin-bottom: 1rem;
    border: 1px solid var(--backend-color-border-light, rgba(233, 236, 239, 0.5));
    color: var(--backend-color-text-primary, #495057);
    transition: background-color 0.35s ease, border-color 0.35s ease, color 0.35s ease;
}
.meta-item {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
}
.meta-item i {
    color: var(--backend-color-text-secondary, #6c757d);
    transition: color 0.35s ease;
}
.meta-item strong {
    color: var(--backend-color-text-primary, #495057);
    font-weight: 600;
    transition: color 0.35s ease;
}
.empty-state {
    text-align: center;
    padding: 4rem 2rem;
    color: var(--backend-color-text-secondary, #6c757d);
    transition: color 0.35s ease;
}
.empty-state i {
    font-size: 4rem;
    margin-bottom: 1rem;
    opacity: 0.5;
    transition: opacity 0.35s ease, color 0.35s ease;
}
.document-layout {
    display: flex;
    gap: 1rem;
    height: calc(100vh - 300px);
    min-height: 600px;
}
.catalog-sidebar {
    width: 300px;
    min-width: 250px;
    border-radius: 8px;
    padding: 1rem;
    overflow: hidden;
    transition: background-color 0.3s ease, border-color 0.3s ease;
    display: flex;
    flex-direction: column;
}
.document-content-area {
    flex: 1;
    overflow-y: auto;
    overflow-x: hidden;
    min-width: 0;
}
#document-list-container {
    width: 100%;
    max-width: 100%;
    overflow-x: hidden;
    box-sizing: border-box;
}
.document-content-area .row {
    margin-left: -0.75rem;
    margin-right: -0.75rem;
}
.document-content-area .row > [class*="col-"] {
    padding-left: 0.75rem;
    padding-right: 0.75rem;
}
.document-content-area .document-card {
    max-width: 100%;
    box-sizing: border-box;
}
.catalog-tree-container {
    flex: 1;
    min-height: 400px;
    overflow-x: auto;
    overflow-y: auto;
    user-select: none;
    background: transparent;
    transition: background-color 0.3s ease;
    scrollbar-width: thin;
    scrollbar-color: rgba(155, 155, 155, 0.5) transparent;
}
.catalog-tree-container::-webkit-scrollbar {
    width: 6px;
    height: 6px;
}
.catalog-tree-container::-webkit-scrollbar-track {
    background: transparent;
    border-radius: 3px;
}
.catalog-tree-container::-webkit-scrollbar-thumb {
    background: rgba(155, 155, 155, 0.5);
    border-radius: 3px;
    transition: background 0.2s ease;
}
.catalog-tree-container::-webkit-scrollbar-thumb:hover {
    background: rgba(155, 155, 155, 0.7);
}
.catalog-tree-container.dragging {
    cursor: grabbing !important;
}
.jstree-node {
    min-height: 28px;
    line-height: 28px;
    margin: 1px 0;
    border-radius: 4px;
    transition: all 0.2s ease;
}
.jstree-node:nth-child(even) {
    background-color: var(--backend-color-bg-secondary, #f8f9fa);
}
.jstree-node:nth-child(odd) {
    background-color: var(--backend-color-card-bg, #ffffff);
}
.jstree-anchor {
    padding: 4px 8px;
    border-radius: 4px;
    transition: all 0.2s ease;
    color: var(--backend-color-text-primary, #495057);
}
.jstree-anchor:hover {
    background-color: rgba(var(--backend-color-primary-rgb, 11, 177, 151), 0.1) !important;
    color: var(--backend-color-primary, #0bb197) !important;
    box-shadow: 0 2px 4px rgba(var(--backend-color-primary-rgb, 11, 177, 151), 0.15);
    transform: translateX(2px);
}
.jstree-clicked {
    background-color: rgba(var(--backend-color-primary-rgb, 11, 177, 151), 0.15) !important;
    color: var(--backend-color-primary, #0a9d85) !important;
    box-shadow: 0 2px 6px rgba(var(--backend-color-primary-rgb, 11, 177, 151), 0.25);
}
.catalog-header {
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--backend-color-border-default, #e9ecef);
    margin-bottom: 1rem;
    transition: border-color 0.3s ease;
    flex-shrink: 0;
}
.catalog-header h5 {
    margin: 0;
    font-size: 1.1rem;
    font-weight: 600;
    color: inherit;
    transition: color 0.3s ease;
}
.jstree-anchor i.mdi {
    font-size: 16px;
    line-height: 1;
    margin-right: 4px;
    display: inline-flex;
    align-items: center;
    flex-shrink: 0;
}
.jstree-contextmenu {
    z-index: 10000;
    border-radius: 6px;
    overflow: hidden;
}
.jstree-contextmenu .jstree-contextmenu-item {
    padding: 10px 20px;
    transition: all 0.2s ease;
}
.jstree-contextmenu .jstree-contextmenu-item:hover {
    background-color: rgba(var(--backend-color-primary-rgb, 11, 177, 151), 0.1);
    color: var(--backend-color-primary, #0bb197);
}
</style>

<!-- 消息 -->
<div class='row'>
    <template>Weline_Component::message.phtml</template>
    <div class='col-12'>
        <div class='card'>
            <div class='card-body'>
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div>
                        <h4 class='card-title mb-1'>
                            <i class="mdi mdi-file-document-outline me-2"></i>
                            <?=__('开发文档')?>
                        </h4>
                        <p class='text-muted mb-0'>
                            <?=__('编写和管理开发文档')?>
                        </p>
                    </div>
                    <div class="d-flex gap-2">
                        <a href="@admin-url('dev/tool/docs/api')">
                            <button type='button' class='btn btn-primary waves-effect waves-light'>
                                <i class="mdi mdi-api me-1"></i>
                                <?=__('API文档')?>
                            </button>
                        </a>
                        <a href="@admin-url('dev/tool/admin/document/catalog')">
                            <button type='button' class='btn btn-info waves-effect waves-light'>
                                <i class="mdi mdi-folder-cog me-1"></i>
                                <?=__('目录管理')?>
                            </button>
                        </a>
                        <a href="@admin-url('dev/tool/admin/document/add')">
                            <button type='button' class='btn btn-success waves-effect waves-light'>
                                <i class="mdi mdi-plus-circle me-1"></i>
                                <?=__('新增文章')?>
                            </button>
                        </a>
                    </div>
                </div>

                <!-- 左右分栏布局 -->
                <div class="document-layout">
                    <!-- 左侧分类树 -->
                    <div class="catalog-sidebar">
                        <div class="catalog-header">
                            <h5>
                                <i class="mdi mdi-folder-tree me-2"></i>
                                <?=__('文档分类')?>
                            </h5>
                        </div>
                        <div id="catalog-tree-container" class="catalog-tree-container">
                            <div class="text-center py-5">
                                <div class="spinner-border text-primary" role="status">
                                    <span class="visually-hidden"><?=__('加载中...')?></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- 右侧文档列表 -->
                    <div class="document-content-area">
                        <div id="document-list-container">
                            <!-- 文档列表 -->
                            <div class="row">
                    <empty name="documents">
                        <div class="col-12">
                            <div class="empty-state">
                                <i class="mdi mdi-file-document-outline"></i>
                                <h5><?=__('暂无文章')?></h5>
                                <p class="text-muted"><?=__('点击"新增文章"按钮开始创建您的第一篇文档')?></p>
                                <a href="@admin-url('dev/tool/admin/document/add')">
                                    <button type='button' class='btn btn-success waves-effect waves-light'>
                                        <i class="mdi mdi-plus-circle me-1"></i>
                                        <?=__('新增文章')?>
                                    </button>
                                </a>
                            </div>
                        </div>
                    </empty>
                    <foreach name="documents" item="vo">
                        <div class="col-md-4 col-lg-3 mb-3">
                            <div class="document-card h-100">
                                <div class="document-header">
                                    <div class="d-flex justify-content-between align-items-start">
                                        <div class="flex-grow-1">
                                            <h5 class="mb-1" style="font-size: 0.95rem; font-weight: 500; line-height: 1.4;">
                                                <var>vo.title</var>
                                            </h5>
                                            <div class="d-flex align-items-center gap-1 mt-1 flex-wrap">
                                                <span class="badge bg-light text-dark" style="font-size: 0.7rem; padding: 0.2rem 0.4rem;">
                                                    <i class="mdi mdi-folder-outline me-1" style="font-size: 0.7rem;"></i>
                                                    <var>vo.c_name</var>
                                                </span>
                                                <if condition="$vo['module_name']">
                                                    <span class="badge-module" style="font-size: 0.7rem; padding: 0.2rem 0.4rem;">
                                                        <i class="mdi mdi-package-variant me-1" style="font-size: 0.7rem;"></i>
                                                        <var>vo.module_name</var>
                                                    </span>
                                                </if>
                                                <if condition="$vo['is_auto_imported']">
                                                    <span class="badge-auto" style="font-size: 0.7rem; padding: 0.2rem 0.4rem;">
                                                        <i class="mdi mdi-auto-fix me-1" style="font-size: 0.7rem;"></i>
                                                        <?=__('自动导入')?>
                                                    </span>
                                                </if>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="document-body">
                                    <p class="summary-text mb-2" style="font-size: 0.8rem; line-height: 1.5;">
                                        <var>vo.summary</var>
                                    </p>
                                    <div class="d-flex justify-content-between align-items-center text-muted" style="font-size: 0.75rem;">
                                        <span>
                                            <i class="mdi mdi-account-outline me-1"></i>
                                            <?=__('作者:')?> <?php echo (($vo['author'] ?? $vo['author_id'] ?? __('未知'))); ?>
                                        </span>
                                        <span>
                                            <i class="mdi mdi-clock-outline me-1"></i>
                                            <var>vo.update_time</var>
                                        </span>
                                    </div>
                                </div>
                                <div class="document-footer">
                                    <div class="action-buttons d-flex justify-content-end">
                                        <button type="button" 
                                                class="btn btn-sm btn-primary view-document-btn" 
                                                data-id="<var>vo.doc_id</var>">
                                            <i class="mdi mdi-eye me-1"></i>
                                            <?=__('查看')?>
                                        </button>
                                        <a href="@admin-url{'dev/tool/admin/document/edit',['id'=>$vo->getData('doc_id')]}">
                                            <button type="button" class="btn btn-sm btn-info">
                                                <i class="mdi mdi-pencil me-1"></i>
                                                <?=__('编辑')?>
                                            </button>
                                        </a>
                                        <if condition="!$vo['is_auto_imported']">
                                        <button type="button" 
                                                class="btn btn-sm btn-danger document-delete-btn" 
                                                data-url="@admin-url{'dev/tool/admin/document/delete',['id'=>$vo->getData('doc_id')]}">
                                            <i class="mdi mdi-delete me-1"></i>
                                            <?=__('删除')?>
                                        </button>
                                        </if>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </foreach>
                </div>

                            <!-- 分页 -->
                            <?php
                            $pagination = $this->getData('pagination');
                            if (!empty($pagination) && !empty($pagination['html'])): ?>
                                <div class="mt-4" id="pagination-container">
                                    <?php echo $pagination['html']; ?>
                                </div>
                            <?php endif; ?>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- 文档查看 Offcanvas -->
<div class="offcanvas offcanvas-end offcanvas-document w-50" tabindex="-1" id="documentViewOffcanvas" aria-labelledby="documentViewOffcanvasLabel">
    <div class="offcanvas-header border-bottom">
        <h5 class="offcanvas-title" id="documentViewOffcanvasLabel">
            <i class="mdi mdi-file-document-outline me-2"></i>
            <?=__('文档详情')?>
        </h5>
        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body">
        <div id="documentViewContent">
            <div class="text-center py-5">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden"><?=__('加载中...')?></span>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- jstree js -->
<link rel="stylesheet" href="@static(Weline_Acl::libs/vakata-jstree-3.3.14-0/dist/themes/default/style.min.css)" />
<script src="@static(Weline_Acl::libs/vakata-jstree-3.3.14-0/dist/jstree.min.js)"></script>
<!--tinymce js-->
<script src='@static(Weline_Admin::assets/libs/tinymce/tinymce.min.js)'></script>
<!-- init js -->
<script src='@static(Weline_Admin::assets/js/pages/form-editor.init.js)'></script>
<!-- Responsive Table js -->
<script src='@static(Weline_Admin::assets/libs/admin-resources/rwd-table/rwd-table.min.js)'></script>
<!-- Init js -->
<script src='@static(Weline_Admin::assets/js/pages/table-responsive.init.js)'></script>
<!-- Sweet Alerts js -->
<script src='@static(Weline_Admin::assets/libs/sweetalert2/sweetalert2.min.js)'></script>
<!-- jquery.vectormap map -->
<script src="@static(Weline_Admin::assets/libs/jqvmap/jquery.vmap.min.js)"></script>
<script src="@static(Weline_Admin::assets/libs/jqvmap/maps/jquery.vmap.usa.js)"></script>

<!--国际化字符串-->
<script>
(function() {
    'use strict';
    
    var i18n = {
        'loading': '<?=__('加载中...')?>',
        'loadingDocument': '<?=__('正在加载文档...')?>',
        'loadDocumentFailed': '<?=__('加载文档失败，请重试')?>',
        'loadDocumentFailedRefresh': '<?=__('加载文档失败，请刷新页面重试')?>',
        'category': '<?=__('分类:')?>',
        'uncategorized': '<?=__('未分类')?>',
        'author': '<?=__('作者:')?>',
        'unknown': '<?=__('未知')?>',
        'createTime': '<?=__('创建时间:')?>',
        'updateTime': '<?=__('更新时间:')?>',
        'module': '<?=__('模块:')?>',
        'autoImported': '<?=__('自动导入')?>',
        'noTitle': '<?=__('无标题')?>',
        'noContent': '<?=__('暂无内容')?>'
    };

    var catalogTree = null;
    var currentCategoryId = <?=json_encode($this->getData('selectedCategoryId') ?? '')?>;
    var documentAdminUrls = {
        list: '<?= $this->getBackendUrl('dev/tool/admin/document/list') ?>',
        view: '<?= $this->getBackendUrl('dev/tool/admin/document/view') ?>',
        edit: '<?= $this->getBackendUrl('dev/tool/admin/document/edit') ?>',
        delete: '<?= $this->getBackendUrl('dev/tool/admin/document/delete') ?>'
    };
    
    function initCatalogTree() {
        try {
            if (typeof $.fn.jstree === 'undefined') {
                $('#catalog-tree-container').html('<div class="alert alert-danger m-3"><i class="mdi mdi-alert-circle me-2"></i><strong><?=__('错误')?>:</strong> <?=__('jstree库未加载，请刷新页面重试')?></div>');
                return;
            }
            
            var catalogs = <?=json_encode($this->getData('catalogs') ?? [])?>;
            var treeData = [];
            
            treeData.push({
                id: '',
                text: '<i class="mdi mdi-view-list me-2"></i><?=__('全部文档')?>',
                state: {
                    selected: !currentCategoryId
                },
                icon: false
            });
            
            if (catalogs && Array.isArray(catalogs) && catalogs.length > 0) {
                var catalogTreeData = convertToJSTreeFormat(catalogs);
                treeData = treeData.concat(catalogTreeData);
            }
            
            $('#catalog-tree-container').html('<div id="catalog-tree"></div>');
            
            catalogTree = $('#catalog-tree').jstree({
                'core': {
                    'data': treeData,
                    'themes': {
                        'responsive': true,
                        'variant': 'large',
                        'stripes': false
                    }
                },
                'plugins': []
            }).on('ready.jstree', function() {
                if (currentCategoryId) {
                    catalogTree.jstree('select_node', currentCategoryId.toString());
                } else {
                    catalogTree.jstree('select_node', '');
                }
            }).on('select_node.jstree', function(e, data) {
                var categoryId = data.node.id;
                loadDocumentsByCategory(categoryId);
            });
        } catch (error) {
            console.error('[CatalogTree] 初始化分类树时发生错误:', error);
        }
    }
    
    function convertToJSTreeFormat(data, level) {
        level = level || 1;
        var result = [];
        if (!data || !Array.isArray(data)) {
            return result;
        }
        
        data.forEach(function(item) {
            if (!item || !item.id) return;
            var itemLevel = (item.level !== null && item.level !== undefined) ? parseInt(item.level) : level;
            var itemName = (item.name !== null && item.name !== undefined) ? String(item.name) : '<?=__('未命名分类')?>';
            
            var node = {
                id: String(item.id),
                text: '<i class="mdi mdi-folder me-2"></i>' + escapeHtml(itemName),
                state: {
                    opened: itemLevel <= 2
                },
                icon: false
            };
            
            if (item.nodes && Array.isArray(item.nodes) && item.nodes.length > 0) {
                var children = convertToJSTreeFormat(item.nodes, itemLevel + 1);
                if (children && children.length > 0) {
                    node.children = children;
                }
            }
            
            result.push(node);
        });
        
        return result;
    }
    
    function escapeHtml(text) {
        if (text === null || text === undefined) {
            return '';
        }
        var div = document.createElement('div');
        div.textContent = String(text);
        return div.innerHTML;
    }
    
    function loadDocumentsByCategory(categoryId) {
        currentCategoryId = categoryId;
        
        $('#document-list-container').html('<div class="text-center py-5"><div class="spinner-border text-primary" role="status"><span class="visually-hidden">' + i18n.loading + '</span></div><p class="mt-2 text-muted">' + i18n.loadingDocument + '</p></div>');
        
        $.ajax({
            url: documentAdminUrls.list,
            method: 'GET',
            data: { category_id: categoryId },
            dataType: 'json',
            success: function(response) {
                if (response.code === 200 && response.data) {
                    renderDocumentList(response.data.items, response.data.pagination);
                } else {
                    $('#document-list-container').html('<div class="alert alert-danger"><i class="mdi mdi-alert-circle me-2"></i>' + (response.msg || i18n.loadDocumentFailed) + '</div>');
                }
            },
            error: function() {
                $('#document-list-container').html('<div class="alert alert-danger"><i class="mdi mdi-alert-circle me-2"></i>' + i18n.loadDocumentFailedRefresh + '</div>');
            }
        });
    }
    
    function renderDocumentList(items, pagination) {
        if (!items || items.length === 0) {
            $('#document-list-container').html('<div class="empty-state"><i class="mdi mdi-file-document-outline"></i><h5><?=__('暂无文章')?></h5><p class="text-muted"><?=__('该分类下暂无文档')?></p></div>');
            return;
        }
        
        var html = '<div class="row">';
        items.forEach(function(doc) {
            html += '<div class="col-md-4 col-lg-3 mb-3"><div class="document-card h-100"><div class="document-header"><div class="d-flex justify-content-between align-items-start"><div class="flex-grow-1"><h5 class="mb-1" style="font-size: 0.95rem; font-weight: 500; line-height: 1.4;">' + (doc.title || '') + '</h5><div class="d-flex align-items-center gap-1 mt-1 flex-wrap"><span class="badge bg-light text-dark" style="font-size: 0.7rem; padding: 0.2rem 0.4rem;"><i class="mdi mdi-folder-outline me-1" style="font-size: 0.7rem;"></i>' + (doc.c_name || '') + '</span>' + (doc.module_name ? '<span class="badge-module" style="font-size: 0.7rem; padding: 0.2rem 0.4rem;"><i class="mdi mdi-package-variant me-1" style="font-size: 0.7rem;"></i>' + doc.module_name + '</span>' : '') + (doc.is_auto_imported ? '<span class="badge-auto" style="font-size: 0.7rem; padding: 0.2rem 0.4rem;"><i class="mdi mdi-auto-fix me-1" style="font-size: 0.7rem;"></i>' + i18n.autoImported + '</span>' : '') + '</div></div></div></div><div class="document-body"><p class="summary-text mb-2" style="font-size: 0.8rem; line-height: 1.5;">' + (doc.summary || '') + '</p><div class="d-flex justify-content-between align-items-center text-muted" style="font-size: 0.75rem;"><span><i class="mdi mdi-account-outline me-1"></i>' + i18n.author + ' ' + (doc.author || doc.author_id || i18n.unknown) + '</span><span><i class="mdi mdi-clock-outline me-1"></i>' + (doc.update_time || '') + '</span></div></div><div class="document-footer"><div class="action-buttons d-flex justify-content-end"><button type="button" class="btn btn-sm btn-primary view-document-btn" data-id="' + doc.doc_id + '"><i class="mdi mdi-eye me-1"></i><?=__('查看')?></button><a href="' + documentAdminUrls.edit + '?id=' + doc.doc_id + '"><button type="button" class="btn btn-sm btn-info"><i class="mdi mdi-pencil me-1"></i><?=__('编辑')?></button></a>' + (!doc.is_auto_imported ? '<button type="button" class="btn btn-sm btn-danger document-delete-btn" data-url="' + documentAdminUrls.delete + '?id=' + doc.doc_id + '"><i class="mdi mdi-delete me-1"></i><?=__('删除')?></button>' : '') + '</div></div></div></div>';
        });
        html += '</div>';
        
        if (pagination && pagination.html) {
            html += '<div class="mt-4" id="pagination-container">' + pagination.html + '</div>';
        }
        
        $('#document-list-container').html(html);
        bindDeleteButtons();
    }
    
    function bindDeleteButtons() {
        $('.document-delete-btn').off('click').on('click', function(e) {
            var target = $(e.target).closest('.document-delete-btn');
            var isDark = document.body.getAttribute('data-topbar') === 'dark';
            var swalConfig = {
                title: '<?=__('你确定要操作么？')?>',
                text: '<?=__('操作后你将无法恢复！')?>',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonText: '<?=__('是的，我确定要删除！')?>',
                cancelButtonText: '<?=__('不要，取消吧！')?>',
                confirmButtonClass: 'btn btn-success mt-2',
                cancelButtonClass: 'btn btn-danger ms-2 mt-2',
                buttonsStyling: false,
                customClass: {
                    popup: isDark ? 'swal2-dark' : '',
                    title: isDark ? 'swal2-title-dark' : '',
                    htmlContainer: isDark ? 'swal2-html-dark' : '',
                    confirmButton: isDark ? 'swal2-confirm-dark' : '',
                    cancelButton: isDark ? 'swal2-cancel-dark' : ''
                }
            };
            Swal.fire(swalConfig).then(function (result) {
                if (result.value) {
                    showLoading();
                    $.post({
                        url: target.attr('data-url')
                    }).done(function(res) {
                        hideLoading();
                        if (200 === res.code) {
                            Swal.fire({
                                title: '<?=__('已删除！')?>',
                                text: '<?=__('文章已经被删除了！')?>',
                                confirmButtonText: '<?=__('好的')?>',
                                icon: 'success',
                                customClass: {
                                    popup: isDark ? 'swal2-dark' : '',
                                    title: isDark ? 'swal2-title-dark' : '',
                                    htmlContainer: isDark ? 'swal2-html-dark' : '',
                                    confirmButton: isDark ? 'swal2-confirm-dark' : ''
                                }
                            }).then(function() {
                                if (currentCategoryId) {
                                    loadDocumentsByCategory(currentCategoryId);
                                } else {
                                    window.location.reload();
                                }
                            });
                        } else {
                            Swal.fire({
                                title: '<?=__('删除提示！')?>',
                                text: '<?=__('文章删除失败，请重试！')?>',
                                confirmButtonText: '<?=__('好的')?>',
                                icon: 'error',
                                customClass: {
                                    popup: isDark ? 'swal2-dark' : '',
                                    title: isDark ? 'swal2-title-dark' : '',
                                    htmlContainer: isDark ? 'swal2-html-dark' : '',
                                    confirmButton: isDark ? 'swal2-confirm-dark' : ''
                                }
                            });
                        }
                    });
                }
            });
        });
    }
    
    window.initCatalogTree = initCatalogTree;
    
    $(document).ready(function() {
        initCatalogTree();
        bindDeleteButtons();
    });
    
    $(document).on('click', '.view-document-btn', function() {
        var docId = $(this).data('id');
        var offcanvasElement = document.getElementById('documentViewOffcanvas');
        var offcanvas = new bootstrap.Offcanvas(offcanvasElement);
        
        $('#documentViewContent').html('<div class="text-center py-5"><div class="spinner-border text-primary" role="status"><span class="visually-hidden">' + i18n.loading + '</span></div><p class="mt-2 text-muted">' + i18n.loadingDocument + '</p></div>');
        
        offcanvas.show();
        
        $.ajax({
            url: documentAdminUrls.view,
            method: 'GET',
            data: { id: docId },
            dataType: 'json',
            success: function(response) {
                if (response.code === 200 && response.data) {
                    var doc = response.data;
                    var html = '<div class="document-meta"><div class="meta-item"><i class="mdi mdi-folder-outline"></i><span><strong>' + i18n.category + '</strong> ' + (doc.category || i18n.uncategorized) + '</span></div><div class="meta-item"><i class="mdi mdi-account-outline"></i><span><strong>' + i18n.author + '</strong> ' + (doc.author || i18n.unknown) + '</span></div><div class="meta-item"><i class="mdi mdi-clock-outline"></i><span><strong>' + i18n.createTime + '</strong> ' + (doc.create_time || '-') + '</span></div><div class="meta-item"><i class="mdi mdi-update"></i><span><strong>' + i18n.updateTime + '</strong> ' + (doc.update_time || '-') + '</span></div>' + (doc.module_name ? '<div class="meta-item"><i class="mdi mdi-package-variant"></i><span><strong>' + i18n.module + '</strong> ' + doc.module_name + '</span></div>' : '') + (doc.is_auto_imported ? '<div class="meta-item"><i class="mdi mdi-auto-fix"></i><span class="badge-auto">' + i18n.autoImported + '</span></div>' : '') + '</div><div class="mb-3"><h4 class="mb-2">' + (doc.title || i18n.noTitle) + '</h4>' + (doc.summary ? '<p class="text-muted">' + doc.summary + '</p>' : '') + '</div><pre class="document-content">' + (doc.content ? escapeHtml(doc.content) : '<span class="text-muted">' + i18n.noContent + '</span>') + '</pre>';
                    $('#documentViewContent').html(html);
                } else {
                    $('#documentViewContent').html('<div class="alert alert-danger"><i class="mdi mdi-alert-circle me-2"></i>' + (response.msg || i18n.loadDocumentFailed) + '</div>');
                }
            },
            error: function() {
                $('#documentViewContent').html('<div class="alert alert-danger"><i class="mdi mdi-alert-circle me-2"></i>' + i18n.loadDocumentFailedRefresh + '</div>');
            }
        });
    });
    
    function initDragScroll() {
        var catalogTreeContainer = document.getElementById('catalog-tree-container');
        if (!catalogTreeContainer) return;
        
        var isMouseDown = false;
        var isDragging = false;
        var startX = 0;
        var startY = 0;
        var scrollLeft = 0;
        var scrollTop = 0;
        var dragThreshold = 5;
        
        catalogTreeContainer.addEventListener('mousedown', function(e) {
            if (e.button !== 0) return;
            if (e.target.tagName === 'A' || e.target.closest('a') || e.target.closest('.jstree-anchor') || e.target.closest('.jstree-node')) {
                if (e.target.classList.contains('jstree-ocl') || e.target.closest('.jstree-ocl')) {
                    return;
                }
            }
            isMouseDown = true;
            isDragging = false;
            startX = e.pageX;
            startY = e.pageY;
            scrollLeft = catalogTreeContainer.scrollLeft;
            scrollTop = catalogTreeContainer.scrollTop;
            e.preventDefault();
        });
        
        document.addEventListener('mousemove', function(e) {
            if (!isMouseDown || !catalogTreeContainer) return;
            if (!isDragging) {
                var deltaX = Math.abs(e.pageX - startX);
                var deltaY = Math.abs(e.pageY - startY);
                if (deltaX > dragThreshold || deltaY > dragThreshold) {
                    isDragging = true;
                    catalogTreeContainer.classList.add('dragging');
                    var rect = catalogTreeContainer.getBoundingClientRect();
                    startX = e.pageX - rect.left;
                    startY = e.pageY - rect.top;
                    scrollLeft = catalogTreeContainer.scrollLeft;
                    scrollTop = catalogTreeContainer.scrollTop;
                } else {
                    return;
                }
            }
            e.preventDefault();
            var rect = catalogTreeContainer.getBoundingClientRect();
            var x = e.pageX - rect.left;
            var y = e.pageY - rect.top;
            var walkX = (x - startX) * 2;
            var walkY = (y - startY) * 2;
            catalogTreeContainer.scrollLeft = scrollLeft - walkX;
            catalogTreeContainer.scrollTop = scrollTop - walkY;
        });
        
        document.addEventListener('mouseup', function() {
            if (isMouseDown) {
                isMouseDown = false;
                if (isDragging) {
                    isDragging = false;
                    catalogTreeContainer.classList.remove('dragging');
                }
            }
        });
        
        catalogTreeContainer.addEventListener('mouseleave', function() {
            if (isMouseDown) {
                isMouseDown = false;
                if (isDragging) {
                    isDragging = false;
                    catalogTreeContainer.classList.remove('dragging');
                }
            }
        });
    }
    
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', function() {
            setTimeout(initDragScroll, 500);
        });
    } else {
        setTimeout(initDragScroll, 500);
    }
})();
</script>
<script>
(function () {
    if (!/\/en_US(?:\/|$)/.test(window.location.pathname)) {
        return;
    }
    const dict = {
        '开发文档': 'Developer Docs',
        '编写和管理开发文档': 'Write and manage developer documents',
        '视图切换': 'View Switcher',
        '卡片视图': 'Card View',
        '卡片': 'Cards',
        '列表视图': 'List View',
        '列表': 'List',
        '翻译配置': 'Translation Config',
        'API文档': 'API Docs',
        '目录管理': 'Catalog Management',
        '新增文章': 'New Article',
        '文档分类': 'Document Categories',
        '加载中...': 'Loading...',
        '暂无文章': 'No articles',
        '点击"新增文章"按钮开始创建您的第一篇文档': 'Click "New Article" to create your first document',
        '自动导入': 'Auto Imported',
        '作者:': 'Author:',
        '查看': 'View',
        '编辑': 'Edit',
        '删除': 'Delete',
        '文档详情': 'Document Details',
        '全部文档': 'All Documents',
        '该分类下暂无文档': 'No documents in this category'
    };
    function replaceNodeText(node) {
        const text = node.nodeValue;
        const trimmed = text.trim();
        if (!trimmed || !dict[trimmed]) {
            return;
        }
        node.nodeValue = text.replace(trimmed, dict[trimmed]);
    }
    function translate(root) {
        const walker = document.createTreeWalker(root || document.body, NodeFilter.SHOW_TEXT);
        const nodes = [];
        while (walker.nextNode()) {
            nodes.push(walker.currentNode);
        }
        nodes.forEach(replaceNodeText);
        document.querySelectorAll('[title],[aria-label],[placeholder]').forEach(function (el) {
            ['title', 'aria-label', 'placeholder'].forEach(function (attr) {
                const value = el.getAttribute(attr);
                if (value && dict[value.trim()]) {
                    el.setAttribute(attr, dict[value.trim()]);
                }
            });
        });
    }
    translate(document.body);
    new MutationObserver(function (mutations) {
        mutations.forEach(function (mutation) {
            mutation.addedNodes.forEach(function (node) {
                if (node.nodeType === Node.TEXT_NODE) {
                    replaceNodeText(node);
                } else if (node.nodeType === Node.ELEMENT_NODE) {
                    translate(node);
                }
            });
        });
    }).observe(document.body, {childList: true, subtree: true});
})();
</script>
