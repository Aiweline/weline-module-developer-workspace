<?php
/**@var \Weline\Framework\View\Template $this */
$apis = $this->getData('apis') ?? [];
?>
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?=__('API文档')?></title>
    <!-- Bootstrap CSS -->
    <link href="@static(Weline_Admin::backend/lib/bootstrap-5.1.3-dist/css/bootstrap.min.css)" rel="stylesheet">
    <!-- Material Design Icons -->
    <link rel="stylesheet" href="@static(Weline_Admin::assets/css/icons.min.css)">
    <!-- Swagger UI -->
    <link rel="stylesheet" type="text/css" href="@static(Weline_DeveloperWorkspace::libs/swagger-ui/5.32.6/swagger-ui.css)" />
    <script src="@static(Weline_DeveloperWorkspace::libs/swagger-ui/5.32.6/swagger-ui-bundle.js)"></script>
    <script src="@static(Weline_DeveloperWorkspace::libs/swagger-ui/5.32.6/swagger-ui-standalone-preset.js)"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background: #f5f7fa;
        }
        
        .api-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        .api-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            border-radius: 10px;
            margin-bottom: 30px;
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
        }
        
        .api-header h1 {
            font-size: 32px;
            margin-bottom: 10px;
        }
        
        .api-header p {
            font-size: 16px;
            opacity: 0.9;
        }
        
        .api-module {
            background: white;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        }
        
        .api-module-title {
            font-size: 24px;
            color: #333;
            margin-bottom: 15px;
            padding-bottom: 10px;
            border-bottom: 2px solid #667eea;
        }
        
        .api-version {
            margin-left: 20px;
            margin-bottom: 15px;
        }
        
        .api-version-title {
            font-size: 18px;
            color: #666;
            margin-bottom: 10px;
        }
        
        .api-class {
            margin-left: 40px;
            margin-bottom: 15px;
        }
        
        .api-class-title {
            font-size: 16px;
            color: #888;
            margin-bottom: 10px;
        }
        
        .api-method {
            margin-left: 60px;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 5px;
            margin-bottom: 10px;
            border-left: 4px solid #667eea;
        }
        
        .api-method-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 10px;
        }
        
        .api-method-badge {
            padding: 4px 8px;
            border-radius: 4px;
            font-size: 12px;
            font-weight: bold;
            color: white;
        }
        
        .api-method-badge.get {
            background: #61affe;
        }
        
        .api-method-badge.post {
            background: #49cc90;
        }
        
        .api-method-badge.put {
            background: #fca130;
        }
        
        .api-method-badge.delete {
            background: #f93e3e;
        }
        
        .api-method-path {
            font-family: monospace;
            color: #333;
        }
        
        .api-method-summary {
            color: #666;
            margin-top: 5px;
        }
        
        #swagger-ui {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="api-container">
        <div class="api-header">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h1><?=__('API文档')?></h1>
                    <p><?=__('Weline Framework API接口文档')?></p>
                </div>
                <div>
                    <a href="<?= $this->getUrl('dev/tool/docs') ?>" class="btn btn-light">
                        <i class="mdi mdi-file-document-outline me-1"></i>
                        <?=__('开发文档')?>
                    </a>
                </div>
            </div>
        </div>
        
        <?php 
        $error = $this->getData('error') ?? null;
        if (!empty($error)): ?>
            <div class="api-module">
                <div class="alert alert-danger">
                    <i class="mdi mdi-alert-circle me-2"></i>
                    <strong><?=__('错误')?>:</strong> <?= htmlspecialchars($error ?? '') ?>
                </div>
            </div>
        <?php endif; ?>
        
        <?php if (empty($apis)): ?>
            <div class="api-module">
                <p><?=__('暂无API文档')?></p>
                <p class="text-muted"><?=__('请运行文档扫描命令导入API文档: php bin/w doc:import')?></p>
            </div>
        <?php else: ?>
            <?php foreach ($apis as $moduleName => $versions): ?>
                <div class="api-module">
                    <h2 class="api-module-title"><?= htmlspecialchars($moduleName ?? '') ?></h2>
                    
                    <?php foreach ($versions as $version => $classes): ?>
                        <div class="api-version">
                            <h3 class="api-version-title"><?= htmlspecialchars($version ?? '') ?></h3>
                            
                            <?php foreach ($classes as $className => $methods): ?>
                                <div class="api-class">
                                    <h4 class="api-class-title"><?= htmlspecialchars($className ?? '') ?></h4>
                                    
                                    <?php foreach ($methods as $api): ?>
                                        <div class="api-method">
                                            <div class="api-method-header">
                                                <span class="api-method-badge <?= strtolower($api['route']['method'] ?? 'get') ?>">
                                                    <?= strtoupper($api['route']['method'] ?? 'GET') ?>
                                                </span>
                                                <span class="api-method-path"><?= htmlspecialchars($api['route']['path'] ?? '') ?></span>
                                            </div>
                                            <div class="api-method-summary">
                                                <?= htmlspecialchars($api['document']['summary'] ?? '') ?>
                                            </div>
                                        </div>
                                    <?php endforeach; ?>
                                </div>
                            <?php endforeach; ?>
                        </div>
                    <?php endforeach; ?>
                </div>
            <?php endforeach; ?>
        <?php endif; ?>
    </div>
    <!-- Bootstrap JS -->
    <script src="@static(Weline_Admin::backend/lib/bootstrap-5.1.3-dist/js/bootstrap.bundle.min.js)"></script>
    <w:seo slot="inspector"/>
</body>
</html>

